import { expect } from "chai";
import { assert } from "chai";

describe("333 first test see if we can do aything ", function () {
  it("333 runs a blank test ", function () {
    assert.strictEqual(2 + 3, 5);
    // expect(2 + 2).to.be(4);
  });
});
